import Util from './util'

let util = null

beforeEach(() => {
  util = new Util()
})

describe('Util 测试', () => {
  test('a 测试', () => {
    // expect(util.a()).toBe()
  })

  test('b 测试', () => {
    // expect(util.a()).toBe()
  })
})
